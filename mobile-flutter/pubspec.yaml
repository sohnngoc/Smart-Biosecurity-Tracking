name: smart_biosecurity_app
description: "Hệ Thống Theo Dõi An Toàn Sinh Học Thông Minh"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.4
  sqflite: ^2.3.0
  path_provider: ^2.1.3
  connectivity_plus: ^6.0.3
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  mqtt_client: ^10.4.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
